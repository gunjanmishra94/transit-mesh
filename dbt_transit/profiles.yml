dbt_transit:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', '../duckdb_data/transit.duckdb') }}"
      threads: 4
      extensions:
        - spatial  # needed to read the GEOMETRY column on raw_vg250_* (choropleth marts)
